# Stripe API Keys
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# Webhook Secret (get this from Stripe CLI or Dashboard)
# For local development, use: stripe listen --forward-to localhost:3000/api/webhook
STRIPE_WEBHOOK_SECRET=whsec_...

# Your domain (for production, use your actual domain)
NEXT_PUBLIC_DOMAIN=http://localhost:3000

# Optional: Stripe Price ID if you want to use an existing price
# NEXT_PUBLIC_STRIPE_PRICE_ID=price_...

